{
  "name": "earlyenglishbooks-mongodb",
  "version": "1.0.0",
  "description": "A mongodb database with node js web service to host early english books headers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "libraries hacked",
  "license": "MIT",
  "dependencies": {
    "filequeue": "^0.5.0",
    "mongodb": "^4.0.1",
    "request": "^2.88.2",
    "xml2js": "^0.5.0"
  }
}
